# 运行指南.md —— 如何运行整个项目

本指南完整说明如何运行「AI 智能客服系统（RAG）」：既支持 **Docker 全栈一键部署**，也支持 **本地开发**（conda 或纯 venv），并给出全量环境变量、模型/API 配置与常见问题。开发者快速上手简版见 `backend/README.md` 与 `frontend/README.md`。

---

## 1. 环境要求

| 组件                     | 版本要求             | 说明                                                   |
| ------------------------ | -------------------- | ------------------------------------------------------ |
| 操作系统                 | WSL2 / Linux（推荐） | 项目针对 WSL2 做了宿主机 IP 探测适配                   |
| Docker                   | 24+（Compose V2）    | 全栈部署必需；确认`docker compose`（中间有空格）可用 |
| Python                   | 3.11                 | 本地开发后端；仅本地路径需要                           |
| Node.js                  | 20+                  | 本地开发前端；仅本地路径需要                           |
| NVIDIA Container Toolkit | 可选                 | 仅 GPU 机器需要，WSL2 自带支持                         |

> Docker 全栈路径**无需本地 conda / Python / Node**——依赖已打进镜像。

---

## 2. 配置：backend/src/.env

后端与 MySQL 容器通过 `backend/src/.env` 读取配置（compose 用 `env_file` 引用它）。先复制模板再编辑：

```bash
cp backend/src/.env.example backend/src/.env
```

**至少填写以下必填项**（其余保持默认即可跑通）：

| 变量                    | 必填原因                                          |
| ----------------------- | ------------------------------------------------- |
| `DeepSeek_API_KEY`    | 不填则问答无法调用 LLM                            |
| `MYSQL_PASSWORD`      | MySQL 业务库密码（占位符必须替换）                |
| `MYSQL_ROOT_PASSWORD` | MySQL root 密码（容器初始化用，占位符必须替换）   |
| `JWT_SECRET`          | 令牌签名密钥，需 ≥32 位随机串，否则登录/注册 500 |

其它要点：

- `SEED_DOCS_ENABLED` 默认 `1`：启动自动初始化内置测试数据（demo 账号与示例知识库）。
- `HF_HUB_OFFLINE` **首启保持 `0`**（在线）：让 Embedding / Reranker 模型联网下载；缓存后再置 `1` 走离线。
- **小说解构变量**：`NOVEL_*` / `DECONSTRUCT_*` 全为可选（有默认值），解构开箱即用；需要调并发、切分阈值、超时或开启 validator 时才需改（完整清单见 §5.1）。
- **解构运行依赖**：小说解构仅需 **MySQL + DeepSeek**（复用同一把 `DeepSeek_API_KEY`），**不依赖 Milvus / GPU**；`EMBEDDING_MODEL_NAME` 等向量相关变量只影响 RAG 问答。

---

## 3. 本地开发启动

### 3.1 安装依赖

**方式 A：conda（推荐）**

```bash
conda env create -f backend/src/environment.yml   # 名称为 Smart-Customer-Service
conda activate Smart-Customer-Service
```

`environment.yml` 委托 pip 安装 `backend/requirements.txt`（全部 Python 依赖）。

**方式 B：无 conda（venv + pip）**

```bash
cd backend
python3.11 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
```

> 无 conda 时系统库（libgcc/libstdcxx/openssl/sqlite）由操作系统提供，现代发行版自带；缺则 `apt install libgcc-s1 libstdc++6 libsqlite3-0`。

验证环境（确认解释器/依赖装对）：

```bash
cd backend/src
python GetEnvInfo.py    # 期望 langchainVersion 1.3.13，pythonExecutable 指向当前虚拟环境
```

### 3.2 启动基础设施（MySQL + Milvus）

```bash
docker compose -f DEPLOY/docker-compose.mysql-only.yml up -d    # MySQL，127.0.0.1:3306
docker compose -f DEPLOY/docker-compose.milvus-only.yml up -d   # Milvus 三组件 + Attu，127.0.0.1:19530
```

### 3.3 启动后端

```bash
cd backend/src
uvicorn main_fastapi:app --host 0.0.0.0 --port 8000
```

启动按顺序完成 5 步初始化：MCP 子进程会话 → Milvus 集合自愈 → LLM 客户端 → 建表 → 内置测试数据（`SEED_DOCS_ENABLED=1` 时）。

> **小说解构**：`/api/novel/*` 解构接口随后端一起就绪，无需额外基础设施。小说上传后到 `/deconstruct` 工作台发起解构即可（使用见 §8）；解构用同一把 `DeepSeek_API_KEY` 调 LLM，不依赖 Milvus。

### 3.4 启动前端

```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:5173 ；`/api` 请求由 Vite 代理转发到后端 8000 端口。

---

## 4. Docker 全栈部署

### 4.1 首次部署

在仓库根目录执行：

```bash
# ① 配置
cp backend/src/.env.example backend/src/.env

# ② 构建镜像
docker compose -f DEPLOY/docker-compose.yml build
# 有 GPU 时叠加：
docker compose -f DEPLOY/docker-compose.yml -f DEPLOY/docker-compose.gpu.yml build

# ③ 启动
docker compose -f DEPLOY/docker-compose.yml up -d

# ④ 确认模型下载完成
docker compose -f DEPLOY/docker-compose.yml logs -f backend
```

等待日志出现：

```
Model loading complete
✅ 服务依赖初始化完成
```

> 此时内置测试数据会自动初始化，demo 账号 `12345678910 / 1234567` 可直接登录体验。

```bash
# ⑤ 可选：模型缓存后开启离线模式
#    编辑 backend/src/.env，取消注释 HF_HUB_OFFLINE=1，然后：
docker compose -f DEPLOY/docker-compose.yml restart backend
```

> 后端首次构建需拉取 torch 等大依赖（含 CUDA 轮子），耗时较长属正常。

### 4.2 调试模式（仅启动依赖组件）

```bash
# 仅 Milvus 三组件 + Attu（本地连 127.0.0.1:19530，面板 http://localhost:3000）
docker compose -f DEPLOY/docker-compose.milvus-only.yml up -d

# 仅 MySQL（本地连 127.0.0.1:3306）
docker compose -f DEPLOY/docker-compose.mysql-only.yml up -d
```

### 4.3 日常命令

```bash
# 停止（安全，保留数据卷）
docker compose -f DEPLOY/docker-compose.yml down
# ❌ 禁止 down -v：会永久删除所有数据卷

# 日志
docker compose -f DEPLOY/docker-compose.yml logs -f            # 全部
docker compose -f DEPLOY/docker-compose.yml logs --tail 100 -f backend   # 后端

# 重启单个服务
docker compose -f DEPLOY/docker-compose.yml restart backend

# 重建镜像（依赖变动时）
docker compose -f DEPLOY/docker-compose.yml build --no-cache backend
docker compose -f DEPLOY/docker-compose.yml up -d
```

> 容器名前缀随 compose 项目名（仓库根运行则为 `006gaizao-*`）；MySQL 因 `container_name` 固定为 `006gaizao-mysql`。建议用 `docker compose ... logs <service>` 而非裸 `docker logs`。

### 4.4 服务访问与健康检查

| 服务      | 地址                       | 说明                                                                                   |
| --------- | -------------------------- | -------------------------------------------------------------------------------------- |
| 前端页面  | http://localhost           | 对话 / 知识库 / 爬虫 / 解构工作台（`/deconstruct`，5 tab：解构/复核/图谱/百科/数据） |
| 后端 API  | http://localhost:8000      | FastAPI 接口                                                                           |
| API 文档  | http://localhost:8000/docs | Swagger 调试页面                                                                       |
| Attu 管理 | http://localhost:3000      | Milvus 可视化面板                                                                      |
| MySQL     | 127.0.0.1:3306             | Navicat /`mysql` 客户端连接                                                          |
| demo 账号 | `12345678910 / 1234567`  | 内置测试数据                                                                           |

```bash
docker compose -f DEPLOY/docker-compose.yml ps   # 预期所有服务 healthy
curl http://localhost/api/health                  # {"status":"ok"}
```

### 4.5 存量数据迁移（可选）

若已有旧 Milvus 向量数据（`milvus-standalone` 等容器），把 `milvus-etcd-data` / `milvus-minio-data` / `milvus-standalone-data` 三个卷复制到新 volume（`deploy_*`），再启动完整集群。详细步骤见 `DEPLOY/打包运行执行说明.md`。

---

## 5. 全量环境变量表

### 5.1 后端（backend/src/.env，全部参数）

标 ⚠️ 为**必填**。

**LLM**

| 参数                      | 默认值                       | 说明                             |
| ------------------------- | ---------------------------- | -------------------------------- |
| `DeepSeek_API_KEY` ⚠️ | —                           | DeepSeek 调用密钥（OpenAI 兼容） |
| `DeepSeek_API_URL`      | `https://api.deepseek.com` | DeepSeek API 地址                |

**Embedding**

| 参数                     | 默认值                                     | 说明                                              |
| ------------------------ | ------------------------------------------ | ------------------------------------------------- |
| `EMBEDDING_MODEL_NAME` | `BAAI/bge-small-zh-v1.5`                 | 中文 Embedding 模型（首启联网下载）               |
| `VEC_DIM`              | `512`                                    | 向量维度，必须与模型输出维度及 Milvus schema 一致 |
| `EMBEDDING_DEVICE`     | `None`                                   | 推理设备；None=自动检测（GPU/cpu）                |
| `QUERY_PREFIX`         | `为这个句子生成表示以用于检索相关文章：` | BGE 中文查询前缀                                  |

**HuggingFace**

| 参数               | 默认值    | 说明                                    |
| ------------------ | --------- | --------------------------------------- |
| `HF_HUB_OFFLINE` | `False` | 离线模式；首启置 0 联网下载，缓存后置 1 |

**Milvus**

| 参数                                  | 默认值                     | 说明                                              |
| ------------------------------------- | -------------------------- | ------------------------------------------------- |
| `AUTO_GET_WIN_HOST_IP`              | `True`                   | WSL2 自动探测宿主 IP                              |
| `MANUAL_MILVUS_HOST` ⚠️           | `xxxxxxxxxxxx`（占位符） | 探测失败时手动指定地址                            |
| `MILVUS_PORT` ⚠️                  | `xxxx`（占位符）         | Milvus 端口（默认 19530；Docker 由 compose 覆盖） |
| `MILVUS_USER` / `MILVUS_PASSWORD` | 空                         | 有鉴权时填写                                      |

**集合**

| 参数                        | 默认值                | 说明                                |
| --------------------------- | --------------------- | ----------------------------------- |
| `CONTENT_COLLECTION_NAME` | `content_knowledge` | 内容知识库集合名                    |
| `CONTENT_COLLECTION_DESC` | `文档内容知识库`    | 集合描述                            |
| `ENABLE_DYNAMIC_FIELD`    | `False`             | 是否启用动态字段                    |
| `AUTO_ID`                 | `False`             | 是否自动生成主键                    |
| `FORCE_REBUILD`           | `False`             | True=每次启动删集合重建（仅测试期） |

**天气（MCP 工具）**

| 参数                | 默认值                     | 说明              |
| ------------------- | -------------------------- | ----------------- |
| `WEATHER_API_KEY` | `your_api_key`（占位符） | 和风天气 API Key  |
| `WEATHER_API_URL` | `your_api_url`（占位符） | 和风天气 API 地址 |

**MySQL**

| 参数                         | 默认值                            | 说明                                            |
| ---------------------------- | --------------------------------- | ----------------------------------------------- |
| `MYSQL_HOST`               | `127.0.0.1`                     | MySQL 地址（Docker 由 compose 覆盖为`mysql`） |
| `MYSQL_PORT`               | `3306`                          | MySQL 端口                                      |
| `MYSQL_USER`               | `ai_customer`                   | 业务库账号                                      |
| `MYSQL_PASSWORD` ⚠️      | `your_mysql_password`（占位符） | 业务库密码                                      |
| `MYSQL_DB`                 | `ai_customer_service`           | 业务库名                                        |
| `MYSQL_ROOT_PASSWORD` ⚠️ | `your_root_password`（占位符）  | root 密码（容器初始化用）                       |
| `MYSQL_DATABASE`           | `ai_customer_service`           | 容器自动建库名                                  |

**JWT**

| 参数                   | 默认值                                  | 说明               |
| ---------------------- | --------------------------------------- | ------------------ |
| `JWT_SECRET` ⚠️    | `your_jwt_secret_32plus_chars_random` | 令牌密钥，≥32 位  |
| `JWT_EXPIRE_MINUTES` | `1440`                                | 令牌有效期（分钟） |

**问答链路**

| 参数                        | 默认值   | 说明                           |
| --------------------------- | -------- | ------------------------------ |
| `PROMPT_OPTIMIZE_ENABLED` | `1`    | 是否启用问题优化               |
| `TOP_K_RETRIEVE`          | `50`   | 向量召回条数                   |
| `TOP_K_RERANK`            | `5`    | 重排序返回条数                 |
| `DAILY_QUOTA`             | `100`  | 每用户每日提问上限（超限 429） |
| `MAX_QUESTION_LEN`        | `500`  | 单次提问最大字数               |
| `CONTEXT_MAX_CHARS`       | `3000` | 多轮上下文字符预算             |
| `HISTORY_RECENT_TURNS`    | `10`   | 保留最近 N 轮                  |
| `FALLBACK_COPY`           | 兜底话术 | 空检索兜底文案                 |
| `RAG_MIN_COSINE_SIM`      | `0.0`  | 检索质量门控（0=关闭）         |

**小说解构（NOVEL / DECONSTRUCT）**

| 参数                            | 默认值    | 说明                                                           |
| ------------------------------- | --------- | -------------------------------------------------------------- |
| `NOVEL_CHAPTER_MAX_CHARS`     | `10000` | 超长章节场景切分阈值（字符，≈8k token）                       |
| `NOVEL_AGENT_MAX_CONCURRENCY` | `5`     | LangGraph 图最大并发（LLM 限流）                               |
| `NOVEL_RECURSION_LIMIT`       | `200`   | 图最大 superstep 数（覆盖默认 25，防大书多章 Send 扇出误触发） |
| `NOVEL_AGENT_MAX_SHRINK`      | `2`     | 单个 Agent 单场景最大缩窗重试次数                              |
| `NOVEL_CHAPTER_LEASE_SECONDS` | `1800`  | 僵死章节租约阈值（processing 超时 → 复位 pending 重认领）     |
| `NOVEL_DECONSTRUCT_ON_UPLOAD` | `0`     | 上传是否默认自动解构（0=否；前端显式 deconstruct=1）           |
| `NOVEL_VALIDATOR_ENABLED`     | `0`     | 是否启用 Layer 2 validator_agent 批处理                        |
| `NOVEL_VALIDATOR_BATCH`       | `50`    | validator 批处理大小                                           |
| `NOVEL_SCENE_MAX`             | `100`   | timeline global_sort 合成的场景上限基数                        |
| `DECONSTRUCT_LLM_TEMPERATURE` | `0.0`   | 解构分析型 LLM 温度（低随机，保证结构化抽取稳定）              |
| `DECONSTRUCT_LLM_TIMEOUT`     | `120`   | 分析型 LLM 超时（秒）                                          |
| `DECONSTRUCT_LLM_MAX_TOKENS`  | `4096`  | 分析型 LLM 输出上限                                            |

**加分项**

| 参数                          | 默认值     | 说明                    |
| ----------------------------- | ---------- | ----------------------- |
| `INTENT_ENABLED`            | `1`      | 意图识别开关            |
| `INTENT_MODE`               | `hybrid` | rule / llm / hybrid     |
| `INTENT_TIMEOUT`            | `8`      | 意图 LLM 兜底超时（秒） |
| `FOLLOWUP_ENABLED`          | `1`      | 追问建议开关            |
| `FOLLOWUP_SUGGESTION_COUNT` | `3`      | 追问条数                |
| `FOLLOWUP_TIMEOUT`          | `15`     | 追问超时（秒）          |
| `SESSION_AUTO_TITLE`        | `1`      | 会话自动命名            |

**日志 / 文档编码**

| 参数                         | 默认值                                        | 说明             |
| ---------------------------- | --------------------------------------------- | ---------------- |
| `LOG_LEVEL`                | `INFO`                                      | 日志级别         |
| `LOG_FORMAT`               | `%(asctime)s - %(levelname)s - %(message)s` | 日志格式         |
| `ENCODING_TRY_ORDER`       | `["utf-8","gb18030","utf-16","latin-1"]`    | 文档编码探测顺序 |
| `ENCODING_DETECTION_BYTES` | `4096`                                      | 探测采样字节数   |
| `MIN_CONTENT_LENGTH`       | `10`                                        | 最小内容长度     |
| `ENCODING_CONFIDENCE`      | `0.7`                                       | 编码判定置信度   |
| `DEFAULT_ENCODING`         | `utf-8`                                     | 默认编码         |

**内置测试数据**

| 参数                  | 默认值                  | 说明                                            |
| --------------------- | ----------------------- | ----------------------------------------------- |
| `SEED_DOCS_ENABLED` | `1`                   | 启动是否初始化内置测试数据（置 0 关闭）         |
| `SEED_DOCS_DIR`     | `../docs/start_files` | 示例文档目录（Docker 下为`/app/start_files`） |

### 5.2 前端（frontend/.env）

| 参数              | 说明                                                                                                                            |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `VITE_API_BASE` | **未被代码使用**（`src/api/index.js` 的 `BASE` 恒为空串）；后端地址由 Vite 代理（开发）或 Nginx（生产）决定，改它无效 |

---

## 6. 模型 / API 配置

- **LLM（DeepSeek）**：在 `backend/src/.env` 填 `DeepSeek_API_KEY`。默认模型 `deepseek-v4-flash`。
- **Embedding / Reranker（BGE）**：`EMBEDDING_MODEL_NAME`（bge-small-zh-v1.5）与 `bge-reranker-v2-m3` 首次启动从 HuggingFace 下载，缓存到 `huggingface_cache` 卷（`~/.cache/huggingface`）；后续可 `HF_HUB_OFFLINE=1` 离线。
- **无 API Key 的替代（本地 Ollama）**：若没有 DeepSeek Key，可改用本地 Ollama 运行开源模型（如 Llama 3 / Qwen2），把 `DeepSeek_API_URL` 指向 Ollama 的 OpenAI 兼容端点、`DeepSeek_API_KEY` 填 `ollama`；Embedding 侧也可用 `OllamaEmbeddingAdapter`（`create_embedding_adapter("ollama")`）。

---

## 7. 内置测试数据（demo 账号）

系统内置一组**测试数据**（对应笔试题「数据初始化建议」：预置示例文档 + 自动向量化，启动后可直接测试问答）：

- **demo 账号**：手机号 `12345678910` / 密码 `1234567`（`backend/scripts/init_db.sql` 预置，幂等）
- **示例知识库**：`docs/start_files/` 下 3 份文档（公司产品介绍.txt、常见问题FAQ.md、退换货政策.txt），启动时经真实导入路径（documents 状态机 → MCP `RAG_init_collection` → Milvus）导入到该用户
- **控制**：`SEED_DOCS_ENABLED`（默认 1，置 0 关闭）、`SEED_DOCS_DIR`；幂等（重启不重复导入）

用 demo 账号登录即可直接问答体验，无需先手工上传文档。

---

## 8. 小说解构使用（解构子系统）

> 小说解构把一部小说逐章解构成结构化知识图谱（实体/关系/时间线/伏笔/冲突/规则），入口为前端 `/deconstruct` 解构工作台（5 tab：解构 / 复核 / 图谱 / 百科 / 数据）。**仅需 MySQL + DeepSeek，不依赖 Milvus。**

### 8.1 上传小说与触发解构

1. **上传小说**：在知识库页面上传小说文件（.txt/.md），系统先按章节切分入库（`novel_chapter`）。
2. **自动触发**：上传时若带 `deconstruct=1`（或环境变量 `NOVEL_DECONSTRUCT_ON_UPLOAD=1`），上传完成后自动发起解构任务。
3. **手动一键解构**：到 `/deconstruct` 工作台选中该书 →「解构」tab → 点击**一键解构**。若已有任务在运行，会提示冲突（HTTP 409）。

### 8.2 查看进度

- 解构任务列表可看到每本书的历史任务（触发方式 / 进度 / 终态）。
- 任务详情展示**实时进度流**：任务启动 → 逐章、逐场景、逐 Agent（8 个解构 Agent）→ 章节完成/失败 → 任务终态；界面同时每秒刷新已完成/失败/总章节计数。进度事件由 `events.py` 事件总线广播、SSE 按 job_id 过滤推送（实现见《AI架构设计.md》§25）。

### 8.3 重试失败章

某章解构失败会在任务详情中标红并附错误原因。可对失败章**重试**：重试全部失败章，或勾选指定章节单独重试。断点续传：任务中断后再次运行只重跑未完成章节。

### 8.4 浏览解构产物

- **图谱 tab**：以实体为中心的关系图（1-hop），支持点选切换中心实体、章节滑块回放。
- **百科 tab**：实体卡 **L0-L4 五区**（身份锚点 / 静态基线 / 当前状态 / 聚合弧光 / 伏笔规则·明暗）+ **三态徽标**（原文直证/合理推断/待复核）+ 置信度弱视觉 + 快照演化时间轴（含突破/转折成长线）+ 出场热力图。
- **数据 tab**：10 类结构化数据（实体 / 快照 / 关系 / 时间线 / 地点 / 伏笔 / 冲突 / 规则 / 别名 / 校验问题）分页浏览与字段筛选。
- 三个视图共享**章节滑块**：拖动到第 N 章即回放到"截至第 N 章"的状态（时态 as-of N）。

### 8.5 人工复核

解构中拦截的可疑项进入「复核」tab（`validation_issue`，pending）：查看待办与汇总 → 点击疑点看**原文证据分屏**（±200 字窗口）→ 裁决（通过 / 忽略 / 修正），或批量确认低风险项 / 批量 re-persist。

---

## 9. 常见问题 FAQ

- **没有 API Key 怎么办？** 可改用本地 Ollama 运行开源模型（Llama 3 / Qwen2），无需付费 API；配置方式见「模型 / API 配置」章节。
- **首次启动卡在模型下载？** `HF_HUB_OFFLINE` 保持 `0` 联网下载一次；网络受限可先手动缓存模型到 `huggingface_cache` 卷。
- **WSL2 连不上 Milvus？** 系统自动探测宿主 IP 并加入 `no_proxy`；失败请在 `.env` 手动设置 `MANUAL_MILVUS_HOST`。
- **`docker compose down -v` 会怎样？** 永久删除所有数据卷（向量集合、模型缓存、数据库），**禁止在存量数据环境执行**；安全停止用 `docker compose down`。
- **`docker compose` 报错？** 本项目用 Compose V2（`docker compose`）；旧版 `docker-compose`（带横线）会忽略 `condition: service_healthy`。
- **前端 API 502？** 后端未启动，或 Vite 代理目标不是 `localhost:8000`（改 `vite.config.js` 的 `proxy.target`）。
- **爬虫不可用？** 动态抓取需 Playwright 浏览器内核（Docker 镜像已含；本地 `playwright install chromium`）。
